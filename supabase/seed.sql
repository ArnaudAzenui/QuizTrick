-- Optional seed for local manual testing. Requires an existing auth user;
-- replace the UUID below with the id from Authentication → Users.
-- Run after 0001_initial_schema.sql.

-- \set owner '00000000-0000-0000-0000-000000000000'

-- insert into public.study_tasks (owner_id, subject, description, estimated_minutes)
-- values (:'owner', 'BIO-110 Chapter 4', 'Review organelles and ATP', 45);
